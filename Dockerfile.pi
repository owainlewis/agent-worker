FROM node:24-slim

RUN npm install -g @mariozechner/pi-coding-agent@latest

# pi needs git for worktree operations
RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

ENTRYPOINT ["pi"]
